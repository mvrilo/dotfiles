#!/usr/bin/env ruby

gems = %q[boom wirble awesome_print hirb lolcat gifme]
system("gem install #{gems}")

packages = %q[picture-tube urban]
system("npm update && npm install #{packages}")

formula = %q[fortune spark]
system("brew update && brew install #{formula}")
